This repo contains the deja vu fonts, in go-bindata format, so that they can
be compiled into Go programs, without having to store the large binary files 
that are relatively static in the same source code repo.
